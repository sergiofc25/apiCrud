@using MudBlazor
@using MudBlazor.Dialog
@using Model.DTO.v1

<MudDialog>
    <DialogContent>
        <MudText>¿Está seguro que desea inhabilitar el Proyecto @Proyecto?.Pre_Nombre?</MudText>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel"
                   Variant="Variant.Text"
                   Color="Color.Error">
            Cancelar
        </MudButton>
        <MudButton OnClick="Submit"
                   Variant="Variant.Filled"
                   Color="Color.Secondary">
            Confirmar
        </MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter]
    MudDialogInstance MudDialog { get; set; }

    [Parameter]
    public DTO_Presupuesto_Obten_Paginado Proyecto { get; set; }

    void Cancel() => MudDialog.Cancel();
    void Submit() => MudDialog.Close(DialogResult.Ok(true));
}